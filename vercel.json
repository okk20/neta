{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "ignoreCommand": "",
  "functions": {
    "api/**": {
      "runtime": "edge"
    }
  },
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/$1" },
    { "source": "/(.*)", "destination": "/" }
  ]
}
